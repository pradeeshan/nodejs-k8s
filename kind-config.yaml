# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080  # Port inside the KinD container
        hostPort: 30080       # Port on your local machine
        protocol: TCP
